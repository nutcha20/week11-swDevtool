version: '3'

services:
  backend:
    build:
      context: ./Back-End
      dockerfile: Dockerfile
    image: it63070048/my-backend_image:1.0
  frontend:
    build:
      context: ./Front-End
      dockerfile: Dockerfile
    image: it63070048/my-frontend_image:1.0
